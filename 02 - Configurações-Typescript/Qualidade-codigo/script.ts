// As opções abaixo são opções de configuração que podem ser uteis e utilizadas como boa pratica

// "noUnusedLocals": true, : informa que uma variavel foi declarar mas não esta sendo usada (só funciona em variaveis que estão dentro de um escopo isolado como uma função, variavel, etc, e não global)

// "noUnusedParameters": true, : avisa que existe um parametro que não está sendo utilizado

// "noImplicitReturns": true,  : não deixa que declare returnos de função que não estejam implicitos, por exemplo returnos q podem ou não ser returnado, ou q tenha mais de uma opção de retorno

